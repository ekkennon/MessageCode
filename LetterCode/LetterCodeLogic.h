#pragma once
#include <string>
#include <vector>
#include <sstream>
#include <iterator>

using namespace std;
using namespace System;

ref class LetterCodeLogic
{
public:
	static string Encode(string);
	static string Decode(vector<int>);
	static string DecodeS(string);
private:
};

